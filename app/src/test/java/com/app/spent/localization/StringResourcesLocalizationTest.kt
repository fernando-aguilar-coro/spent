package com.app.spent.localization

import org.junit.Ignore
import org.junit.Test

/**
 * Localization unit tests cancelled per user request.
 */
@Ignore("Unit tests cancelled per user request")
class StringResourcesLocalizationTest {

    @Test
    fun testLocalizationIgnored() {
        // Cancelled per user request
    }
}
